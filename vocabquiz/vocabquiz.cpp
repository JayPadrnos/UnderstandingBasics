struct VocabItem {
    std::string nativeword;
    std::string foreignWord;
    std::string pronunciation;
    std::string exampleSentence;
}

// struct for japanese language needs work and other questionare for archetype
// redo the flash card make it for the hiragana and katana symbols so that you can identify easier set
// up something like duolingo??? so user can view all at once and choose which one to even hear if clicked on
// set it up like a learning game?